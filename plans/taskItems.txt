
setup .gitignore
X .env X
X secrets X

setup eslint
X install as dev dep 
X npx eslint --init

X write initial structure
X add  (not important for now)
X add new and email
X email untouched    (may not be needed)

X setup and link to remote on github

X write the form
X write the splitting function
X write the state preview

create custom properties (role category, opening sentence)
check what custom properties are there
create custom properties if needed
