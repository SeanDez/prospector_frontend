
X setup .gitignore
X .env X
X secrets X

X setup eslint
X install as dev dep 
X npx eslint --init

// write initial structure
X add  (not important for now)
X add new and email
X email untouched    (may not be needed)

X setup and link to remote on github

// write the form
X write the splitting function
X write the state preview

// create custom properties (role category, opening sentence)
X check what custom properties are there
X create custom properties if needed

X setup titlecasing and uppercasing

// add contacts
X figure out what info needs to ship to back end
X create interface. include other important core details (lead status, )
setup a fetch to back end
X on back end, setup an authed api request
catch response on front end
flash positive response on front end for 1-2 seconds
X setup add via ctrl+enter

// send gmail


// add email event to contact


// add contact option selections
X add state variables
X tie state to UI






